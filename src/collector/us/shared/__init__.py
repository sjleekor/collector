"""modeler/가 쓰는 유일한 공개 표면.

여기 없는 것은 modeler/에서 import하지 않는다. 넓히려면 그 판단을 여기 적는다.
순수 데이터와 순수 함수만 내보낸다 — DB도 HTTP도 설정도 건드리지 않는다.
한국 쪽 같은 자리는 collector.kr.shared다 (심볼 26개).

아직 비어 있다. 미국 수집 로직이 생기면 채운다.
"""
